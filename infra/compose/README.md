# infra/compose

Docker Compose stack for the isolated Stage A launch on the shared VPS, project name
`hasheemstudio`, project-specific networks/volumes only. Not yet implemented — see
`docs/IMPLEMENTATION-PLAN.md` Phase 2 and `docs/ARCHITECTURE.md` "Docker launch and Kubernetes
path".
