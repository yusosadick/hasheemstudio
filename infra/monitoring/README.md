# infra/monitoring

Prometheus/Grafana/alerting config scoped to Hasheem Studio, integrating with existing monitoring
via scoped credentials only (never shared admin credentials for the host's existing
Prometheus/Grafana/Loki stack). Not yet implemented.
