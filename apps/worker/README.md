# apps/worker

TypeScript + BullMQ media worker orchestrating pinned FFmpeg/FFprobe via argument arrays. Not yet
implemented — see `docs/STATUS.md` (Phase 4: probe/remux; Phase 5: compatibility encode).
