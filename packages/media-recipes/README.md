# packages/media-recipes

Versioned recipe definitions (inspect / remux / compatibility encode / platform presets) shared
between API (for display) and worker (for execution), per `docs/ARCHITECTURE.md` "Recipe
semantics". Not yet implemented.
